export type Stat = {
    value: string
    label: string
}

export type BoardCard = {
    id: string
    patient: string
    treatment: string
    dentist: string
    due: string
    badge: string
    badgeClass: string
}
